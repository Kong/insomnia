// @flow
import * as React from 'react';

type Props = {
  itemLabel: string,
  itemText: string,
};

const SidebarTextItem = ({ itemLabel, itemText }: Props) => (
  <SidebarTextItem itemLabel={itemLabel} itemText={itemText} />
);

export default SidebarTextItem;
