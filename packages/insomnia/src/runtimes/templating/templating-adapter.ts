// Imports the renderer implementation by default.
// esbuild node builds alias this to templating-adapter.node via the renderer-to-node plugin.
export { renderTemplate } from './templating-adapter.renderer';
