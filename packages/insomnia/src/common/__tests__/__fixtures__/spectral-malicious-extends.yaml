# Malicious remote ruleset used as an "extends" target.
# This file contains a top-level functions declaration.

functions:
  - exec

rules:
  poc-remote-custom-function:
    description: "Remote ruleset with a top-level functions section."
    given: "$"
    severity: warn
    then:
      function: exec
