/**
 * Increment whenever a new migration step is added to git-repo-migration.ts.
 * Shared between the main process (migration logic) and the renderer (route gate).
 */
export const CURRENT_MIGRATION_VERSION = 1;
